Bu depo, **Berlino** isimli basit bir Solidity sözleşmesini içerir.  
Kodun amacı, bir karşılama mesajını (greeting) saklamak, güncellemek ve görüntülemektir.  

Arc projesine **SmartDeployContract** yapmak için tasarlanmıştır.  
Lütfen kodları aşağıdaki **kopyalama butonuna tıklayarak** kopyalayın ve kendi `.sol` dosyanızı oluşturup kodun içeriğini değiştirin.

---

```solidity
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.30;

contract Berlino {
    string private greeting;

    constructor() {
        greeting = "Hello Berlino!";
    }

    function setGreeting(string memory newGreeting) public {
        greeting = newGreeting;
    }

    function getGreeting() public view returns (string memory) {
        return greeting;
    }
}
